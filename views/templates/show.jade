extends ../layout

block content
  .page-header
    h1= title
    p.lead
      | Feel free to 
      a(href="/templates/#{record.id}/edit") edit it
      |  if you need to.    

  dl.dl-horizontal.lead
    dt Name
    dd= record.name
    dt Type
    dd= record.type
    dt Body
    dd= record.body
    dt Available?
    dd= record.availableForUse    